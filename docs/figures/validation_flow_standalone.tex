% Standalone wrapper for rendering the shared validation-flow diagram to
% an image (svg for README.md). The Data Sheet and User Guide \input the
% .tikz fragment directly; this wrapper exists only for the rendered copy.
%   pdflatex validation_flow_standalone.tex
%   pdftocairo -svg validation_flow_standalone.pdf validation_flow.svg
\documentclass[tikz,border=6pt]{standalone}
\usetikzlibrary{arrows.meta,positioning}
\definecolor{FStatBlue}{HTML}{144E8C}
\definecolor{LightGray}{HTML}{F3F3F3}
% \linewidth has no meaning in a standalone tikz page; give the blocks a
% concrete width equivalent to the in-document rendering.
\setlength{\linewidth}{11cm}
\begin{document}
\input{validation_flow.tikz}
\end{document}
