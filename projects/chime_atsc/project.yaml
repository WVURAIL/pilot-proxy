# Project profile: CHIME baseband archive, ATSC UHF television as the emitter.
# Data only. pilot_proxy.config.project loads this directory; the Python
# modules hold no project values. Paths are relative to this directory.
name: chime_atsc
instrument: chime                        # pilot_proxy/instruments/chime.yaml
detector_adapter: narrowband_marker
frequency_plan: frequency_plan.yaml
interference_template: interference_template.yaml
detector_config: detector_config.yaml
integration_model: integration_model.yaml
eras:
  author_eras: eras/author_eras_2026-09-23.json
  transmitter_off: eras/transmitter_off.json
  states: [proxy-high, proxy-low, ambiguous-only]
register: detector_register.json
